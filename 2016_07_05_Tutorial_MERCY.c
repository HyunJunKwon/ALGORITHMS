// Header files inclusion
#include <stdio.h>

int main(void)
{
	// Local variables declare
	int num;

	// Statements
	scanf_s("%d", &num);
	while ((num--) > 0)
	{
		printf("Hello Algospot!\n");
	}
	return 0;
}